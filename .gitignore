# deps
node_modules

# next
.next
out

# env
.env
.env.*

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# vercel
.vercel

# data artifacts
# Keep latest.json committed for demo/submission reproducibility.
# (If it gets too noisy later we can switch to build-time generation.)
